<!-- spirit-level house rules.

Everything in this file is injected into EVERY session, regardless of which
model is driving. Use it for rules that are about your setup rather than
about model behavior: which docs tool to check first, where your project
context lives, which skills are mandatory for which task types.

Keep it short. This text is paid for on every single turn.

Delete the comment and the example below, then write your own. An empty file
injects nothing.

Example:

STANDING ORDERS:
- Library/API work -> check the docs tool FIRST, before writing anything
- Error cause unknown -> pull the trace or git log FIRST, not a guess
- "Works" = end-to-end tested and confirmed by a human. Nothing else counts.
- Prose -> invoke the writing skill FIRST | UI -> invoke the design skill FIRST
- Need project context -> check the memory directory FIRST, not the user
-->
